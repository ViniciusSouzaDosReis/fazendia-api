import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    paddingVertical: 16,
    zIndex: 99
  },
  absolute: {
    position: 'absolute',
    top: 32,
    left: 16
  }
});

export default styles